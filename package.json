{
  "scripts": {
    "build-css": "sass scss/main.scss css/style.css --watch",
    "serve": "live-server --watch=.",
    "start": "npm-run-all --parallel build-css serve"
  },
  "dependencies": {
    "sass": "^1.78.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5"
  }
}
